<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LexVanguard | Attorneys & Counselors at Law</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        :root {
            --navy: #000000;
            --navy-light: #1a1a1a;
            --gold: #EAB308;
            --gold-hover: #ca8a04;
            --gray-light: #f4f4f4;
            --gray-dark: #333333;
            --text-main: #ffffff;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #000000;
            color: var(--text-main);
            overflow-x: hidden;
            line-height: 1.6;
        }

        h1, h2, h3, h4, h5, .font-serif {
            font-family: 'Montserrat', sans-serif;
        }

        /* Traditional Buttons */
        .btn-gold {
            background-color: var(--gold);
            color: white;
            padding: 12px 24px;
            font-family: 'Open Sans', sans-serif;
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: none;
            transition: background-color 0.3s ease;
            cursor: pointer;
            display: inline-block;
        }

        .btn-gold:hover {
            background-color: var(--gold-hover);
        }

        .btn-navy {
            background-color: var(--navy);
            color: white;
            padding: 12px 24px;
            font-family: 'Open Sans', sans-serif;
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: none;
            transition: background-color 0.3s ease;
            cursor: pointer;
            display: inline-block;
        }

        .btn-navy:hover {
            background-color: var(--navy-light);
        }

        /* Traditional Practice Boxes */
        .practice-box {
            background-color: var(--gray-light);
            border-top: 4px solid var(--navy);
            padding: 40px 30px;
            text-align: center;
            transition: all 0.3s ease;
            color: #333;
        }

        .practice-box:hover {
            background-color: white;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
            transform: translateY(-5px);
        }

        .practice-box h3 {
            margin-top: 20px;
            margin-bottom: 15px;
            font-size: 24px;
            color: var(--navy);
        }

        .practice-box p {
            font-size: 15px;
            color: #666;
            margin-bottom: 20px;
        }

        /* Footer Links */
        .footer-link {
            color: #b0b0b0;
            text-decoration: none;
            transition: color 0.2s;
            font-size: 14px;
            display: block;
            margin-bottom: 10px;
        }
        
        .footer-link:hover {
            color: var(--gold);
        }

        /* Fade Animation */
        .fade-in {
            animation: fadeIn 1s ease-in forwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Hide elements dynamically */
        .hidden-page {
            display: none;
        }
    </style>
</head>
<body>

    <!-- NAVIGATION HEADER -->
    <header id="main-header" class="absolute top-0 left-0 w-full z-50 bg-gradient-to-b from-black/80 to-transparent">
        <div class="max-w-[1600px] mx-auto px-6 py-6 flex justify-between items-start">
            
            <!-- Logo -->
            <div class="flex flex-col cursor-pointer border-l-[3px] border-[var(--gold)] pl-3 mt-2" onclick="navigate('home')">
                <span class="text-3xl md:text-4xl font-extrabold text-white leading-none tracking-wide">LEX<br/>VANGUARD</span>
                <span class="text-[9px] md:text-[10px] tracking-[0.15em] text-gray-400 uppercase mt-1">Counsels at Law</span>
            </div>

            <!-- Right Side Nav -->
            <div class="flex flex-col items-end mt-1">
                <!-- Top Bar - Contact & Search -->
                <div class="flex items-center mb-5">
                    <i data-lucide="phone" class="text-white mr-3 w-4 h-4 fill-current"></i>
                    <div class="h-5 w-px bg-white/50 mr-3"></div>
                    <span class="text-[var(--gold)] font-bold text-lg md:text-xl tracking-wider mr-6">+254116171396</span>
                    <button class="border-2 border-white p-1 hover:bg-white hover:text-black transition-colors group">
                        <i data-lucide="search" class="w-4 h-4 group-hover:text-black" stroke-width="3"></i>
                    </button>
                </div>

                <!-- Desktop Links -->
                <nav class="hidden lg:flex space-x-7 items-center">
                    <button onclick="navigate('home')" class="nav-link text-[13px] font-bold text-white hover:text-[var(--gold)] transition-colors">Our Firm</button>
                    <button onclick="navigate('attorneys')" class="nav-link text-[13px] font-bold text-white hover:text-[var(--gold)] transition-colors">Attorneys</button>
                    <button onclick="navigate('services')" class="nav-link text-[13px] font-bold text-white hover:text-[var(--gold)] transition-colors">Practice Areas</button>
                    <button onclick="navigate('history')" class="nav-link text-[13px] font-bold text-white hover:text-[var(--gold)] transition-colors">History</button>
                    <button onclick="navigate('news')" class="nav-link text-[13px] font-bold text-white hover:text-[var(--gold)] transition-colors">News and Events</button>
                    <button onclick="navigate('careers')" class="nav-link text-[13px] font-bold text-white hover:text-[var(--gold)] transition-colors">Careers</button>
                    <button onclick="navigate('contact')" class="nav-link text-[13px] font-bold text-white hover:text-[var(--gold)] transition-colors">Contact</button>
                    
                    <!-- Dynamic Auth Links -->
                    <button id="nav-portal-link" onclick="navigate('dashboard')" class="hidden text-[13px] font-bold text-[var(--gold)] hover:text-white transition-colors">Portal</button>
                </nav>

                <!-- Mobile Menu Toggle -->
                <button class="lg:hidden text-white mt-4" onclick="toggleMobileMenu()">
                    <i data-lucide="menu" id="menu-icon-open" class="w-7 h-7"></i>
                    <i data-lucide="x" id="menu-icon-close" class="w-7 h-7 hidden"></i>
                </button>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden lg:hidden bg-black border-t border-gray-800 text-white absolute w-full left-0 top-full">
            <div class="flex flex-col px-6 py-4 space-y-4">
                <button onclick="navigate('home'); toggleMobileMenu()" class="text-left text-sm font-bold py-2 border-b border-white/10">Our Firm</button>
                <button onclick="navigate('attorneys'); toggleMobileMenu()" class="text-left text-sm font-bold py-2 border-b border-white/10">Attorneys</button>
                <button onclick="navigate('services'); toggleMobileMenu()" class="text-left text-sm font-bold py-2 border-b border-white/10">Practice Areas</button>
                <button onclick="navigate('history'); toggleMobileMenu()" class="text-left text-sm font-bold py-2 border-b border-white/10">History</button>
                <button onclick="navigate('news'); toggleMobileMenu()" class="text-left text-sm font-bold py-2 border-b border-white/10">News and Events</button>
                <button onclick="navigate('careers'); toggleMobileMenu()" class="text-left text-sm font-bold py-2 border-b border-white/10">Careers</button>
                <button onclick="navigate('contact'); toggleMobileMenu()" class="text-left text-sm font-bold py-2 border-b border-white/10">Contact</button>
                
                <button id="mobile-portal-link" onclick="navigate('dashboard'); toggleMobileMenu()" class="hidden text-left text-sm font-bold text-[var(--gold)] py-2 border-b border-white/10">Portal</button>
                <button id="mobile-login-link" onclick="navigate('login'); toggleMobileMenu()" class="text-left text-sm font-bold text-[var(--gold)] py-2 border-b border-white/10">Client Login</button>
                <button id="mobile-logout-link" onclick="logout(); toggleMobileMenu()" class="hidden text-left text-sm font-bold text-red-400 py-2 border-b border-white/10">Logout</button>
            </div>
        </div>
    </header>

    <!-- MAIN CONTENT CONTAINER -->
    <main>
        
        <!-- ==================== HOME PAGE ==================== -->
        <div id="page-home" class="w-full bg-black">
            <!-- Hero Section -->
            <div class="relative h-screen w-full flex items-center overflow-hidden">
                <!-- Slides Container -->
                <div id="hero-slide-0" class="hero-slide absolute inset-0 z-0 transition-opacity duration-1000 ease-in-out opacity-100">
                    <img src="assets/WhatsApp%20Image%202026-03-15%20at%202.49.52%20PM.jpeg" onerror="this.src='https://placehold.co/1920x1080/1a1a1a/EAB308?text=Slide+1'" alt="Slide 1" class="w-full h-full object-cover opacity-80">
                    <div class="absolute inset-0 bg-gradient-to-r from-transparent via-black/40 to-black/90"></div>
                    <div class="relative z-10 w-full h-full max-w-[1600px] mx-auto px-6 pt-32 pb-24 flex items-center justify-end">
                        <div class="w-full sm:w-[80%] md:w-[70%] lg:w-[60%] border-l-[4px] md:border-l-[6px] border-[var(--gold)] pl-4 md:pl-8 lg:pr-20 fade-in">
                            <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-extrabold leading-[1.15] md:leading-[1.1] tracking-tight">
                                <span class="block text-[var(--gold)]">MERGING A</span>
                                <span class="block text-[var(--gold)]">MODERN MINDSET</span>
                                <span class="block text-white">WITH THE PRACTICES WE</span>
                                <span class="block text-white">VALUE</span>
                            </h1>
                        </div>
                    </div>
                </div>

                <div id="hero-slide-1" class="hero-slide absolute inset-0 z-0 transition-opacity duration-1000 ease-in-out opacity-0">
                    <img src="assets/conference.jpeg" onerror="this.src='https://placehold.co/1920x1080/1a1a1a/EAB308?text=Slide+2'" alt="Slide 2" class="w-full h-full object-cover opacity-80">
                    <div class="absolute inset-0 bg-gradient-to-r from-transparent via-black/40 to-black/90"></div>
                    <div class="relative z-10 w-full h-full max-w-[1600px] mx-auto px-6 pt-32 pb-24 flex items-center justify-end">
                        <div class="w-full sm:w-[80%] md:w-[70%] lg:w-[60%] border-l-[4px] md:border-l-[6px] border-[var(--gold)] pl-4 md:pl-8 lg:pr-20 fade-in">
                            <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-extrabold leading-[1.15] md:leading-[1.1] tracking-tight">
                                <span class="block text-[var(--gold)]">PIONEERING</span>
                                <span class="block text-[var(--gold)]">LEGAL RESEARCH</span>
                                <span class="block text-white">AND ELITE APPELLATE</span>
                                <span class="block text-white">ADVOCACY</span>
                            </h1>
                        </div>
                    </div>
                </div>

                <div id="hero-slide-2" class="hero-slide absolute inset-0 z-0 transition-opacity duration-1000 ease-in-out opacity-0">
                    <img src="assets/conference2.jpeg" onerror="this.src='https://placehold.co/1920x1080/1a1a1a/EAB308?text=Slide+3'" alt="Slide 3" class="w-full h-full object-cover opacity-80">
                    <div class="absolute inset-0 bg-gradient-to-r from-transparent via-black/40 to-black/90"></div>
                    <div class="relative z-10 w-full h-full max-w-[1600px] mx-auto px-6 pt-32 pb-24 flex items-center justify-end">
                        <div class="w-full sm:w-[80%] md:w-[70%] lg:w-[60%] border-l-[4px] md:border-l-[6px] border-[var(--gold)] pl-4 md:pl-8 lg:pr-20 fade-in">
                            <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-extrabold leading-[1.15] md:leading-[1.1] tracking-tight">
                                <span class="block text-[var(--gold)]">ENTERPRISE-GRADE</span>
                                <span class="block text-[var(--gold)]">LEGAL COUNSEL</span>
                                <span class="block text-white">FOR TOMORROW'S</span>
                                <span class="block text-white">CHALLENGES</span>
                            </h1>
                        </div>
                    </div>
                </div>

                <!-- Bottom Floating Bar -->
                <div class="absolute bottom-10 left-0 w-full px-6 md:px-10 z-20 flex justify-between items-end">
                    <button class="border-2 border-[var(--gold)] text-white w-10 h-10 flex items-center justify-center hover:bg-[var(--gold)] hover:text-black transition-colors">
                        <span class="font-serif font-bold text-xl italic">i</span>
                    </button>

                    <button class="absolute left-1/2 transform -translate-x-1/2 bottom-0 flex items-center bg-black/40 border border-transparent hover:border-white/20 px-6 py-3 text-white font-bold text-sm tracking-widest transition-all">
                        <i data-lucide="chevrons-down" class="w-4 h-4 text-[var(--gold)] mr-3"></i> EXPLORE
                    </button>

                    <div class="hidden md:flex items-center text-white space-x-6 text-sm font-bold tracking-widest">
                        <button onclick="prevSlide()" class="cursor-pointer hover:text-[var(--gold)] transition-colors"><i data-lucide="arrow-left" class="w-5 h-5"></i></button>
                        <div class="flex items-center space-x-2" id="slider-indicator">
                            <span class="text-[var(--gold)]">01</span>
                            <span class="text-gray-400">/</span>
                            <span class="text-white">03</span>
                        </div>
                        <button onclick="nextSlide()" class="cursor-pointer hover:text-[var(--gold)] transition-colors"><i data-lucide="arrow-right" class="w-5 h-5"></i></button>
                    </div>
                </div>
            </div>

            <!-- Intro Section -->
            <div class="py-20 bg-white">
                <div class="max-w-4xl mx-auto px-4 text-center">
                    <h2 class="text-4xl font-serif mb-6 text-[var(--navy)]">Welcome to LexVanguard</h2>
                    <p class="text-gray-600 leading-loose text-lg mb-8">
                        Founded on the principles of rigorous academic inquiry and uncompromising advocacy, LexVanguard is a premier law firm delivering enterprise-grade legal services. We bridge the gap between traditional legal theory and high-stakes, modern practice, securing favorable outcomes in complex litigation and corporate matters.
                    </p>
                    <img src="https://placehold.co/1200x400/f4f4f4/000000?text=Intro+Section+Image+(E.g.,+Courthouse)" alt="Courthouse Placeholder" class="w-full h-[400px] object-cover mb-8 border-4 border-[var(--gray-light)]">
                </div>
            </div>

            <!-- Core Philosophy Grid -->
            <div class="bg-[var(--gray-light)] py-20">
                <div class="max-w-7xl mx-auto px-4">
                    <div class="text-center mb-16">
                        <h2 class="text-4xl font-serif mb-4 text-[var(--navy)]">Our Core Philosophy</h2>
                        <div class="h-1 w-16 bg-[var(--gold)] mx-auto"></div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                        <div class="practice-box">
                            <i data-lucide="compass" class="w-10 h-10 text-[var(--gold)] mx-auto"></i>
                            <h3 class="uppercase text-xl">A Vision for Lasting Change</h3>
                            <p class="text-sm">LEXIVANGUARD stands at the forefront of modern advocacy, driven by a relentless commitment to systemic change. We don't just react to the legal landscape; we actively reshape it to ensure a more equitable future. By combining strategic foresight with a passion for justice, the firm serves as a powerful engine for progress, turning ambitious ideals into tangible societal shifts.</p>
                            <button class="text-[var(--navy)] font-semibold uppercase text-xs tracking-wide hover:text-[var(--gold)] transition-colors bg-transparent border-none cursor-pointer">Learn More &raquo;</button>
                        </div>

                        <div class="practice-box">
                            <i data-lucide="award" class="w-10 h-10 text-[var(--gold)] mx-auto"></i>
                            <h3 class="uppercase text-xl">Unmatched Excellence in Advocacy</h3>
                            <p class="text-sm">As a leader in the field, LEXIVANGUARD has earned its reputation as the gold standard for advocacy. Our team’s expertise is matched only by our dedication to achieving the best possible outcomes for those we represent. This pursuit of excellence ensures that every case we handle and every policy we champion is backed by rigorous intellectual depth and an unwavering spirit of victory.</p>
                            <button class="text-[var(--navy)] font-semibold uppercase text-xs tracking-wide hover:text-[var(--gold)] transition-colors bg-transparent border-none cursor-pointer">Learn More &raquo;</button>
                        </div>

                        <div class="practice-box">
                            <i data-lucide="users" class="w-10 h-10 text-[var(--gold)] mx-auto"></i>
                            <h3 class="uppercase text-xl">Open Doors, Open Solutions</h3>
                            <p class="text-sm">At the heart of our mission is the belief that high-level advocacy should be available to everyone, regardless of background or circumstance. LEXIVANGUARD prides itself on being an accessible, "always-on" resource for the community. We bridge the gap between complex legal structures and the people who need them most, ensuring that our doors remain open and our experts remain ready to serve whenever change is needed.</p>
                            <button class="text-[var(--navy)] font-semibold uppercase text-xs tracking-wide hover:text-[var(--gold)] transition-colors bg-transparent border-none cursor-pointer">Learn More &raquo;</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Attorneys Teaser -->
            <div class="py-20 bg-[var(--navy)] text-white">
                <div class="max-w-7xl mx-auto px-4 flex flex-col md:flex-row items-center justify-between">
                    <div class="md:w-2/3 pr-8 mb-8 md:mb-0">
                        <h2 class="text-4xl font-serif text-white mb-6">Our Attorneys</h2>
                        <p class="text-gray-300 leading-loose text-lg mb-8">
                            Our team comprises distinguished legal professionals, leading academics, and national moot court champions dedicated to providing strategic, result-oriented representation.
                        </p>
                        <button onclick="navigate('attorneys')" class="btn-gold">Meet Our Team</button>
                    </div>
                    <div class="md:w-1/3">
                        <div class="grid grid-cols-2 gap-4">
                            <img src="https://placehold.co/400x400/f4f4f4/000000?text=Attorney+Portrait+1" alt="Attorney Placeholder 1" class="w-full h-40 object-cover border-2 border-[var(--gold)]">
                            <img src="https://placehold.co/400x400/f4f4f4/000000?text=Attorney+Portrait+2" alt="Attorney Placeholder 2" class="w-full h-40 object-cover border-2 border-[var(--gold)]">
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ==================== ATTORNEYS PAGE ==================== -->
        <div id="page-attorneys" class="w-full bg-white pb-20 hidden-page">
            <div class="bg-[var(--navy)] pt-40 pb-20 px-6 text-center border-b-4 border-[var(--gold)]">
                <h1 class="text-4xl md:text-5xl font-serif text-white uppercase tracking-wider fade-in">Our Attorneys</h1>
                <div class="h-1 w-16 bg-[var(--gold)] mx-auto mt-6"></div>
            </div>

            <div class="max-w-4xl mx-auto px-6 py-16 text-center text-[var(--navy)]">
                <p class="text-gray-600 leading-loose text-lg">
                    At LexVanguard, our greatest asset is our exceptional team of legal minds. 
                    From seasoned litigators who have shaped landmark appellate decisions to innovative 
                    strategists guiding the next generation of tech enterprises, our attorneys merge a 
                    modern mindset with the traditional practices we value.
                </p>
            </div>

            <div class="max-w-[1400px] mx-auto px-6 text-[var(--navy)]">
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10" id="attorneys-grid">
                    <!-- Populated by JS -->
                </div>
            </div>
        </div>

        <!-- ==================== DASHBOARD PAGE ==================== -->
        <div id="page-dashboard" class="min-h-screen bg-[var(--gray-light)] py-12 px-4 pt-32 hidden-page text-[var(--navy)]">
            <div class="max-w-7xl mx-auto">
                <div class="bg-white p-8 border-t-4 border-[var(--navy)] shadow-sm mb-8 flex flex-col md:flex-row justify-between items-center">
                    <div>
                        <h1 class="text-3xl font-serif text-[var(--navy)] mb-2">Member Portal Workspace</h1>
                        <p class="text-gray-600 font-semibold" id="dashboard-welcome">
                            Welcome <span class="mx-2 text-gray-300">|</span> Role: 
                        </p>
                    </div>
                    <div class="mt-4 md:mt-0 flex space-x-4">
                        <button class="btn-navy flex items-center space-x-2 text-xs">
                            <i data-lucide="calendar" class="w-4 h-4"></i> <span>Calendar</span>
                        </button>
                        <button class="btn-gold flex items-center space-x-2 text-xs">
                            <i data-lucide="file-text" class="w-4 h-4"></i> <span>New File</span>
                        </button>
                    </div>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                    <!-- Main Content -->
                    <div class="lg:col-span-2 space-y-8">
                        <div class="bg-white p-8 border border-gray-200 shadow-sm">
                            <h3 class="text-2xl font-serif text-[var(--navy)] mb-6 border-b border-gray-200 pb-4">Active Matters</h3>
                            <div class="space-y-0 divide-y divide-gray-200" id="tasks-container">
                                <!-- Populated by JS -->
                            </div>
                        </div>

                        <div class="bg-[var(--navy)] text-white p-8 border-b-4 border-[var(--gold)]">
                            <h3 class="text-2xl font-serif text-white mb-4">LexAI Legal Research Database</h3>
                            <p class="text-gray-300 text-sm mb-6 max-w-xl leading-relaxed">
                                Query precedents, access firm templates, or summarize complex litigation documents utilizing the internal AI-assisted knowledge graph.
                            </p>
                            <div class="flex">
                                <input type="text" placeholder="Enter search query..." class="flex-1 border-none text-gray-900 px-4 py-3 focus:outline-none text-sm">
                                <button class="bg-[var(--gold)] px-6 hover:bg-[var(--gold-hover)] transition-colors flex items-center justify-center font-bold uppercase text-xs tracking-wider">Search</button>
                            </div>
                        </div>
                    </div>

                    <!-- Sidebar -->
                    <div class="space-y-8">
                        <div class="grid grid-cols-2 gap-4">
                            <div class="bg-white p-6 text-center border border-gray-200 shadow-sm">
                                <span class="text-4xl font-serif text-[var(--navy)] block mb-2">12</span>
                                <span class="text-xs text-gray-500 uppercase tracking-widest font-bold">Active Cases</span>
                            </div>
                            <div class="bg-white p-6 text-center border border-gray-200 shadow-sm">
                                <span class="text-4xl font-serif text-[var(--gold)] block mb-2">3</span>
                                <span class="text-xs text-gray-500 uppercase tracking-widest font-bold">Deadlines</span>
                            </div>
                        </div>

                        <div class="bg-white p-8 border border-gray-200 shadow-sm">
                            <h3 class="text-xl font-serif text-[var(--navy)] mb-6 border-b border-gray-200 pb-4">System Alerts</h3>
                            <div class="space-y-6">
                                <div class="flex items-start">
                                    <div class="text-[var(--gold)] mr-4 mt-1"><i data-lucide="file-text" class="w-4 h-4"></i></div>
                                    <div>
                                        <p class="font-bold text-gray-800 text-sm">Appellate Brief Uploaded</p>
                                        <p class="text-xs text-gray-500">By A. Pendelton • Today, 9:00 AM</p>
                                    </div>
                                </div>
                                <div class="flex items-start">
                                    <div class="text-[var(--navy)] mr-4 mt-1"><i data-lucide="check-circle" class="w-4 h-4"></i></div>
                                    <div>
                                        <p class="font-bold text-gray-800 text-sm">Client Intake Form Approved</p>
                                        <p class="text-xs text-gray-500">System • Yesterday, 4:30 PM</p>
                                    </div>
                                </div>
                                <div class="flex items-start">
                                    <div class="text-gray-400 mr-4 mt-1"><i data-lucide="bell" class="w-4 h-4"></i></div>
                                    <div>
                                        <p class="font-bold text-gray-800 text-sm">Meeting Scheduled: Corp Tech</p>
                                        <p class="text-xs text-gray-500">Tomorrow, 10:00 AM</p>
                                    </div>
                                </div>
                            </div>
                            <button class="w-full mt-8 btn-navy bg-gray-100 !text-[var(--navy)] border border-gray-300 hover:bg-gray-200 text-xs">
                                View Full Log
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ==================== ADMIN PAGE ==================== -->
        <div id="page-admin" class="min-h-screen bg-[var(--gray-light)] py-12 px-4 pt-32 hidden-page text-[var(--navy)]">
            <div class="max-w-7xl mx-auto">
                <div class="bg-white p-8 border-t-4 border-[var(--navy)] shadow-sm mb-8">
                    <h1 class="text-3xl font-serif text-[var(--navy)] mb-2">System Administration</h1>
                    <p class="text-gray-600 font-semibold">Personnel & Access Control Management</p>
                </div>

                <div class="bg-white border border-gray-200 shadow-sm">
                    <div class="p-6 border-b border-gray-200 flex justify-between items-center bg-gray-50">
                        <h2 class="text-xl font-serif text-[var(--navy)]">Firm Directory</h2>
                        <button class="btn-gold text-xs py-2 px-4">Add Personnel</button>
                    </div>
                    <div class="overflow-x-auto p-4">
                        <table class="w-full text-left border-collapse">
                            <thead>
                                <tr class="bg-[var(--navy)] text-white text-xs uppercase tracking-widest font-semibold">
                                    <th class="p-4 border border-gray-300">Name</th>
                                    <th class="p-4 border border-gray-300">Role Classification</th>
                                    <th class="p-4 border border-gray-300">Contact Email</th>
                                    <th class="p-4 border border-gray-300 text-center">Action</th>
                                </tr>
                            </thead>
                            <tbody id="admin-users-tbody">
                                <!-- Populated by JS -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- ==================== LOGIN PAGE ==================== -->
        <div id="page-login" class="min-h-[70vh] bg-[var(--gray-light)] flex items-center justify-center py-20 px-4 pt-40 hidden-page">
            <div class="bg-white p-10 w-full max-w-md border-t-4 border-[var(--navy)] shadow-lg">
                <h2 class="text-3xl font-serif text-[var(--navy)] text-center mb-6">Portal Access</h2>
                <p class="text-gray-600 text-center mb-8 text-sm">Please authenticate with your firm credentials to access the Vanguard system.</p>
                
                <div class="space-y-4">
                    <div id="login-error" class="hidden bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4 text-sm" role="alert"></div>
                    <div>
                        <label class="block text-sm font-bold text-gray-700 mb-1" for="email">Email Address</label>
                        <input class="w-full px-3 py-2 text-gray-900 border border-gray-300 focus:outline-none focus:border-[var(--navy)] focus:ring-1 focus:ring-[var(--navy)]" id="email" type="email" placeholder="Enter your email" required>
                    </div>
                    <div>
                        <label class="block text-sm font-bold text-gray-700 mb-1" for="password">Password</label>
                        <input class="w-full px-3 py-2 text-gray-900 border border-gray-300 focus:outline-none focus:border-[var(--navy)] focus:ring-1 focus:ring-[var(--navy)]" id="password" type="password" placeholder="Enter your password" required>
                    </div>
                    <button id="submit-login" class="w-full btn-gold text-center mt-4">Authenticate</button>
                </div>
                
                <div class="mt-8 text-center">
                    <button onclick="navigate('home')" class="text-[var(--gold)] font-bold text-xs uppercase tracking-widest hover:underline bg-transparent border-none cursor-pointer">
                        &laquo; Return to Homepage
                    </button>
                </div>
            </div>
        </div>

    </main>

    <!-- FOOTER -->
    <footer id="main-footer" class="bg-[#111111] text-gray-400 py-16 px-4">
        <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-4 gap-12">
            <div class="col-span-1 md:col-span-1 text-left">
                <h3 class="text-2xl font-serif text-white mb-6 uppercase tracking-widest">LexVanguard</h3>
                <p class="text-sm font-light leading-relaxed text-gray-500 mb-6">
                    A tradition of excellence. A commitment to rigorous and innovative legal strategy across the nation.
                </p>
                <div class="flex space-x-4">
                    <i data-lucide="facebook" class="w-4 h-4 hover:text-white cursor-pointer transition-colors"></i>
                    <i data-lucide="twitter" class="w-4 h-4 hover:text-white cursor-pointer transition-colors"></i>
                    <i data-lucide="linkedin" class="w-4 h-4 hover:text-white cursor-pointer transition-colors"></i>
                </div>
            </div>
            <div>
                <h4 class="text-white font-serif text-lg mb-6 uppercase tracking-wider">Practice Areas</h4>
                <ul class="space-y-2">
                    <li><a href="#" class="footer-link">Corporate & Technology</a></li>
                    <li><a href="#" class="footer-link">Intellectual Property</a></li>
                    <li><a href="#" class="footer-link">Appellate Litigation</a></li>
                    <li><a href="#" class="footer-link">Pro Bono Initiative</a></li>
                </ul>
            </div>
            <div>
                <h4 class="text-white font-serif text-lg mb-6 uppercase tracking-wider">The Firm</h4>
                <ul class="space-y-2">
                    <li><a href="#" class="footer-link">Attorneys & Staff</a></li>
                    <li><a href="#" class="footer-link">Firm History</a></li>
                    <li><a href="#" class="footer-link">Careers</a></li>
                    <li><a href="#" class="footer-link">News & Insights</a></li>
                </ul>
            </div>
            <div>
                <h4 class="text-white font-serif text-lg mb-6 uppercase tracking-wider">Contact</h4>
                <ul class="space-y-4 text-sm font-light">
                    <li class="flex items-start">
                        <i data-lucide="map-pin" class="w-4 h-4 text-[var(--gold)] mr-3 mt-1 shrink-0"></i>
                        <span>123 Legal Plaza, Suite 400<br/>Metropolis, NY 10001</span>
                    </li>
                    <li class="flex items-center">
                        <i data-lucide="phone" class="w-4 h-4 text-[var(--gold)] mr-3 shrink-0"></i>
                        <span>+254116171396</span>
                    </li>
                    <li class="flex items-center">
                        <i data-lucide="mail" class="w-4 h-4 text-[var(--gold)] mr-3 shrink-0"></i>
                        <span>lexvanguard</span>
                    </li>
                </ul>
            </div>
        </div>
        <div class="max-w-7xl mx-auto mt-16 pt-8 border-t border-gray-800 text-xs text-gray-600 flex flex-col md:flex-row justify-between items-center">
            <p>&copy; 2026 LexVanguard. All rights reserved.</p>
            <div class="flex space-x-6 mt-4 md:mt-0 uppercase tracking-widest font-semibold">
                <a href="#" class="hover:text-gray-300 transition-colors">Privacy Policy</a>
                <a href="#" class="hover:text-gray-300 transition-colors">Terms of Use</a>
                <a href="#" class="hover:text-gray-300 transition-colors">Disclaimer</a>
            </div>
        </div>
    </footer>

    <!-- APP LOGIC -->
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
        import { getAuth, signInWithEmailAndPassword, onAuthStateChanged, signOut } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";

        // Firebase Configuration
        const firebaseConfig = {
            apiKey: "AIzaSyASZup8rGHMlvzwCx0Ykjbcc-HWGw_QZYA",
            authDomain: "solutionfied.firebaseapp.com",
            projectId: "solutionfied",
            storageBucket: "solutionfied.firebasestorage.app",
            messagingSenderId: "619009885012",
            appId: "1:619009885012:web:4429fb954a7ee021953ec1",
            measurementId: "G-B07XZZZB5W"
        };

        const app = initializeApp(firebaseConfig);
        const auth = getAuth(app);

        // Data Models
        const ROLES = {
            CLIENT: { level: 0, name: 'Client' },
            MEMBER: { level: 1, name: 'Member' },
            RESEARCHER: { level: 2, name: 'Researcher' },
            ASSOCIATE: { level: 3, name: 'Associate' },
            MANAGER: { level: 5, name: 'Manager' },
            ADMIN: { level: 10, name: 'Admin' },
            MANAGING_PARTNER: { level: 100, name: 'Managing Partner' }
        };

        const PUBLIC_USER = { id: 'public', name: 'Public User', role: ROLES.CLIENT, email: '' };

        // Curated Firm Members Map (Gated Access based on Firebase UIDs)
        const AUTHORIZED_USERS = {
            'n6NKoyAIuVSXYEaIbRVN9drINNy1': { id: 'n6NKoyAIuVSXYEaIbRVN9drINNy1', name: 'Prince Micah', role: ROLES.MANAGING_PARTNER, email: 'prince@lexvanguard.edu' },
            'SSbNEJrVyhM6b8LbWYsyunPGk6l2': { id: 'SSbNEJrVyhM6b8LbWYsyunPGk6l2', name: 'Kelvin Musya', role: ROLES.MANAGING_PARTNER, email: 'kelvin@lexvanguard.edu' }
        };

        const ATTORNEYS = [
            { name: "Prince Micah", title: "Managing Partner", practice: "Corporate & Tech Law, Mergers & Acquisitions", image: "assets/prince.png" },
            { name: "Kelvin Musya", title: "Senior Partner", practice: "Appellate Advocacy, Supreme Court Litigation", image: "assets/kmusya.jpeg" },
            { name: "Donel Aganyo", title: "Partner", practice: "Intellectual Property, Patent Litigation", image: "https://placehold.co/400x500/1a1a1a/EAB308?text=Donel+Aganyo" },
            { name: "Linet Njeri", title: "Finance Manager", practice: "Commercial Litigation, Dispute Resolution", image: "https://placehold.co/400x500/1a1a1a/EAB308?text=Linet+Njeri" },
            { name: "Sharon Mwariri", title: "Lead Legal Researcher", practice: "Policy Analysis, Legislative Drafting", image: "https://placehold.co/400x500/1a1a1a/EAB308?text=Sharon+Mwariri" },
            { name: "Kimathi Winner", title: "Associate", practice: "Pro Bono Initiative, Civil Rights", image: "https://placehold.co/400x500/1a1a1a/EAB308?text=Kimathi+Winner" }
        ];

        const TASKS = [
            { id: 1, title: 'Draft Appellate Brief', status: 'In Progress', priority: 'High', assignee: 'Sarah Jenkins' },
            { id: 2, title: 'M&A Due Diligence Review', status: 'Pending', priority: 'Medium', assignee: 'David Chen' },
            { id: 3, title: 'Client Intake Form - IP Litigation', status: 'Completed', priority: 'High', assignee: 'Sarah Jenkins' }
        ];

        // App State
        let currentUser = PUBLIC_USER;
        const pages = ['home', 'attorneys', 'login', 'dashboard', 'admin'];
        let currentSlide = 0;
        const totalSlides = 3;

        // Initialize UI Elements
        function initApp() {
            renderAttorneys();
            renderTasks();
            lucide.createIcons();
            updateAuthUI();
            
            // Start Slider
            setInterval(nextSlide, 6000);

            // Attach Login Event Listener
            document.getElementById('submit-login').addEventListener('click', handleLoginSubmit);
        }

        // Make functions globally available for inline onclick handlers
        window.navigate = navigate;
        window.toggleMobileMenu = toggleMobileMenu;
        window.logout = handleLogout;
        window.prevSlide = prevSlide;
        window.nextSlide = nextSlide;

        // Navigation
        function navigate(page) {
            // Guard clauses for protected routes
            if (page === 'dashboard' && currentUser.role.level < ROLES.MEMBER.level) page = 'login';
            if (page === 'admin' && currentUser.role.level < ROLES.MANAGER.level) page = 'dashboard';

            // Hide all pages, show target
            pages.forEach(p => {
                const el = document.getElementById(`page-${p}`);
                if (el) {
                    if(p === page) el.classList.remove('hidden-page');
                    else el.classList.add('hidden-page');
                }
            });

            // Footer and Header visibility
            document.getElementById('main-footer').style.display = page === 'login' ? 'none' : 'block';
            if(page === 'login'){
                document.getElementById('main-header').classList.remove('bg-gradient-to-b');
                document.getElementById('main-header').classList.add('bg-[var(--navy)]');
            } else {
                document.getElementById('main-header').classList.add('bg-gradient-to-b');
                document.getElementById('main-header').classList.remove('bg-[var(--navy)]');
            }

            window.scrollTo(0, 0);
        }

        function toggleMobileMenu() {
            const menu = document.getElementById('mobile-menu');
            const openIcon = document.getElementById('menu-icon-open');
            const closeIcon = document.getElementById('menu-icon-close');
            
            if (menu.classList.contains('hidden')) {
                menu.classList.remove('hidden');
                openIcon.classList.add('hidden');
                closeIcon.classList.remove('hidden');
            } else {
                menu.classList.add('hidden');
                openIcon.classList.remove('hidden');
                closeIcon.classList.add('hidden');
            }
        }

        // --- FIREBASE AUTHENTICATION LOGIC ---

        // Securely fetch user profile from curated list and navigate
        async function verifyUserAndNavigate(user) {
            const submitBtn = document.getElementById('submit-login');
            const errorDiv = document.getElementById('login-error');
            
            try {
                // Match the authenticated Firebase user to our curated internal list
                const userData = AUTHORIZED_USERS[user.uid];

                if (userData) {
                    // Dynamically assign role based on curated list
                    currentUser = {
                        id: user.uid,
                        name: userData.name,
                        email: user.email,
                        role: {
                            level: userData.role.level,
                            name: userData.role.name
                        }
                    };
                    
                    updateAuthUI();
                    
                    // Clear login form fields on success
                    document.getElementById('email').value = '';
                    document.getElementById('password').value = '';
                    
                    // Proceed to their specific "office" (dashboard) if they are on the login page
                    if (!document.getElementById('page-login').classList.contains('hidden-page')) {
                         navigate(currentUser.role.level >= ROLES.MEMBER.level ? 'dashboard' : 'home');
                    }
                } else {
                    // User logged in correctly via Firebase, but is NOT on the approved curated list
                    await signOut(auth);
                    currentUser = PUBLIC_USER;
                    if (errorDiv) {
                        errorDiv.textContent = "Access denied. Your account is not authorized for a firm office.";
                        errorDiv.classList.remove('hidden');
                    }
                    updateAuthUI();
                }
            } catch (error) {
                console.error("Error verifying permissions:", error);
                await signOut(auth);
                currentUser = PUBLIC_USER;
                if (errorDiv) {
                    errorDiv.textContent = "A system error occurred while verifying your permissions.";
                    errorDiv.classList.remove('hidden');
                }
                updateAuthUI();
            } finally {
                // Guaranteed to reset the button state regardless of success or failure
                if (submitBtn) {
                    submitBtn.textContent = "Authenticate";
                    submitBtn.disabled = false;
                }
            }
        }

        // Handle Form Submission
        async function handleLoginSubmit() {
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            const errorDiv = document.getElementById('login-error');
            const submitBtn = document.getElementById('submit-login');

            if (!email || !password) {
                errorDiv.textContent = "Please enter both email and password.";
                errorDiv.classList.remove('hidden');
                return;
            }

            try {
                errorDiv.classList.add('hidden');
                submitBtn.textContent = "Authenticating...";
                submitBtn.disabled = true;

                const userCredential = await signInWithEmailAndPassword(auth, email, password);
                
                // Visual cue that Auth succeeded and DB check is starting
                submitBtn.textContent = "Loading Office...";
                
                // Explicitly execute the verification sequence
                await verifyUserAndNavigate(userCredential.user);
                
            } catch (error) {
                console.warn("Login Warning:", error.message);
                
                let errorMessage = "Authentication failed. Please check your credentials.";
                if (error.code === 'auth/invalid-credential' || error.code === 'auth/wrong-password' || error.code === 'auth/user-not-found') {
                    errorMessage = "Invalid email or password. Please make sure this account is registered in your Firebase console.";
                } else if (error.code === 'auth/too-many-requests') {
                    errorMessage = "Too many failed login attempts. Please try again later.";
                }
                
                errorDiv.textContent = errorMessage;
                errorDiv.classList.remove('hidden');
                submitBtn.textContent = "Authenticate";
                submitBtn.disabled = false;
            }
        }

        // Listen to Auth State Changes (Handles page reloads automatically)
        onAuthStateChanged(auth, async (user) => {
            if (user) {
                // Only run automatic check if not actively submitting the login form
                const submitBtn = document.getElementById('submit-login');
                if (!submitBtn || submitBtn.textContent === "Authenticate") {
                    await verifyUserAndNavigate(user);
                }
            } else {
                currentUser = PUBLIC_USER;
                updateAuthUI();
                
                // Protect routes if user logged out while on them
                const isDashboard = !document.getElementById('page-dashboard').classList.contains('hidden-page');
                const isAdmin = !document.getElementById('page-admin').classList.contains('hidden-page');
                if (isDashboard || isAdmin) {
                    navigate('home');
                }
            }
        });

        async function handleLogout() {
            try {
                await signOut(auth);
            } catch (error) {
                console.error("Error signing out:", error);
            }
        }

        function updateAuthUI() {
            // Desktop Links
            const navPortalLink = document.getElementById('nav-portal-link');
            
            // Mobile Links
            const mobilePortalLink = document.getElementById('mobile-portal-link');
            const mobileLoginLink = document.getElementById('mobile-login-link');
            const mobileLogoutLink = document.getElementById('mobile-logout-link');

            if (currentUser.role.level >= ROLES.MEMBER.level) {
                navPortalLink.classList.remove('hidden');
                mobilePortalLink.classList.remove('hidden');
                mobileLoginLink.classList.add('hidden');
                mobileLogoutLink.classList.remove('hidden');
                mobileLogoutLink.innerHTML = `Logout (${currentUser.name})`;
            } else {
                navPortalLink.classList.add('hidden');
                mobilePortalLink.classList.add('hidden');
                mobileLoginLink.classList.remove('hidden');
                mobileLogoutLink.classList.add('hidden');
            }

            // Update Dashboard Welcome text
            const welcomeText = document.getElementById('dashboard-welcome');
            if (welcomeText) {
                welcomeText.innerHTML = `Welcome, ${currentUser.name} <span class="mx-2 text-gray-300">|</span> Role: ${currentUser.role.name}`;
            }
        }

        // Slider Logic
        function updateSliderUI() {
            for (let i = 0; i < totalSlides; i++) {
                const slide = document.getElementById(`hero-slide-${i}`);
                if (slide) {
                    if (i === currentSlide) {
                        slide.classList.remove('opacity-0');
                        slide.classList.add('opacity-100');
                    } else {
                        slide.classList.remove('opacity-100');
                        slide.classList.add('opacity-0');
                    }
                }
            }
            const indicator = document.getElementById('slider-indicator');
            if (indicator) {
                indicator.innerHTML = `
                    <span class="text-[var(--gold)]">${String(currentSlide + 1).padStart(2, '0')}</span>
                    <span class="text-gray-400">/</span>
                    <span class="text-white">${String(totalSlides).padStart(2, '0')}</span>
                `;
            }
        }

        function nextSlide() {
            currentSlide = (currentSlide + 1) % totalSlides;
            updateSliderUI();
        }

        function prevSlide() {
            currentSlide = currentSlide === 0 ? totalSlides - 1 : currentSlide - 1;
            updateSliderUI();
        }

        // Render Functions
        function renderAttorneys() {
            const grid = document.getElementById('attorneys-grid');
            if (!grid) return;
            grid.innerHTML = ATTORNEYS.map(attorney => `
                <div class="group cursor-pointer">
                    <div class="relative overflow-hidden mb-4 border-2 border-[var(--gray-light)] group-hover:border-[var(--gold)] transition-colors duration-300">
                        <img src="${attorney.image}" alt="${attorney.name}" class="w-full h-[400px] object-cover grayscale group-hover:grayscale-0 transition-all duration-500 transform group-hover:scale-105" />
                        <div class="absolute inset-0 bg-black/10 group-hover:bg-transparent transition-colors duration-300"></div>
                    </div>
                    <h3 class="text-2xl font-serif text-[var(--navy)] group-hover:text-[var(--gold)] transition-colors">${attorney.name}</h3>
                    <p class="text-[var(--gold)] font-bold text-sm uppercase tracking-wider mt-1 mb-2">${attorney.title}</p>
                    <p class="text-gray-500 text-sm"><span class="font-semibold text-gray-700">Practices:</span> ${attorney.practice}</p>
                    <button class="mt-4 text-xs font-bold uppercase tracking-widest text-[var(--navy)] border-b-2 border-[var(--navy)] pb-1 group-hover:text-[var(--gold)] group-hover:border-[var(--gold)] transition-all bg-transparent cursor-pointer">View Profile &raquo;</button>
                </div>
            `).join('');
        }

        function renderTasks() {
            const container = document.getElementById('tasks-container');
            if (!container) return;
            container.innerHTML = TASKS.map(task => `
                <div class="py-4 flex flex-col sm:flex-row sm:items-center justify-between">
                    <div class="mb-2 sm:mb-0">
                        <h4 class="font-bold text-gray-800 text-lg">${task.title}</h4>
                        <p class="text-sm text-gray-500">Assigned Attorney: ${task.assignee}</p>
                    </div>
                    <div class="flex items-center space-x-6">
                        <span class="text-sm font-semibold">
                            Status: <span class="${task.status === 'Completed' ? 'text-green-600' : 'text-[var(--gold)]'}">${task.status}</span>
                        </span>
                        <button class="text-[var(--navy)] font-semibold uppercase text-xs tracking-wide hover:text-[var(--gold)] transition-colors bg-transparent border-none cursor-pointer">View Details &raquo;</button>
                    </div>
                </div>
            `).join('');
        }

        // Initialize on load
        document.addEventListener('DOMContentLoaded', initApp);
    </script>
</body>
</html>